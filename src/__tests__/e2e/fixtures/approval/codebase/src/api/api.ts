// Both of these edges are already approved in the diagram.
import { domain } from '../domain/domain.js';
import { shared } from '../shared/shared.js';

export const api = `api(${domain},${shared})`;
